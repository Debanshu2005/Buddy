# ===============================
# Pi_Buddy — Raspberry Pi Requirements
# Install with: pip install -r requirements.txt
# ===============================

# ── Core ────────────────────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.10.0
requests>=2.31.0
python-dotenv>=1.0.0

# ── Computer Vision ──────────────────────────────────────────────────────────
# Use headless build on Pi (no GUI dependencies)
opencv-python-headless>=4.8.0
onnxruntime>=1.16.0

# ── Machine Learning / Face Recognition ──────────────────────────────────────
scikit-learn>=1.4.0
insightface>=0.7.3

# ── Object Detection ─────────────────────────────────────────────────────────
# ultralytics pulls in torch+CUDA which is huge — not needed on Pi.
# The codebase uses YOLOv8 via ONNX directly through onnxruntime.
# Only install these lightweight helpers:
pillow>=10.0.0
pyyaml>=6.0

# ── Speech & Audio ───────────────────────────────────────────────────────────
edge-tts>=6.1.0
websockets>=12.0
vosk>=0.3.45

# ── Behavior / Pose Pipeline ────────────────────────────────────────────────
# mediapipe has no Python 3.13 / ARM64 wheel — disabled by default
# To enable: install Python 3.11, then: pip install mediapipe>=0.10.0
# BUDDY_ENABLE_MEDIAPIPE_BEHAVIOR=0 (default) so this is not needed to run Buddy
# mediapipe>=0.10.0

# ── Hardware (Raspberry Pi only) ─────────────────────────────────────────────
# RPi.GPIO and gpiozero are pre-installed on Raspberry Pi OS
# Uncomment if installing on a fresh venv:
# RPi.GPIO>=0.7.1
# gpiozero>=2.0

# ── Serial (Arduino motor controller) ────────────────────────────────────────
pyserial>=3.5

# ── OLED Display (SSD1306 / SH1106) ──────────────────────────────────────────
luma.oled>=3.12.0

# ── Camera (Pi Camera Module) ────────────────────────────────────────────────
# picamera2 is installed via apt on Raspberry Pi OS, not pip:
#   sudo apt install -y python3-picamera2
# Uncomment only if using a pip-based environment:
# picamera2

# ── Notification / Phone Link ─────────────────────────────────────────────────
# (uses only stdlib + requests — no extra deps)

# ── Database (optional — only if psycopg2 is used) ───────────────────────────
# psycopg2-binary>=2.9.0
